<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>صحتك ولياقتك - رحلتك نحو الجسم المثالي</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #1a2a6c, #b21f1f, #fdbb2d);
            color: #333;
            line-height: 1.6;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        /* الهيدر */
        header {
            text-align: center;
            padding: 20px;
            margin-bottom: 30px;
        }

            header h1 {
                color: white;
                font-size: 2.5rem;
                margin-bottom: 10px;
                text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
            }

            header p {
                color: #f8f8f8;
                font-size: 1.2rem;
                max-width: 800px;
                margin: 0 auto;
            }

        /* الهيرو مع السلايدر */
        .hero-slider {
            position: relative;
            width: 100%;
            height: 500px;
            margin: 0 auto 40px;
            overflow: hidden;
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        .slide {
            position: absolute;
            width: 100%;
            height: 100%;
            opacity: 0;
            transition: opacity 1s ease, transform 1s ease;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 40px;
            text-align: center;
            color: white;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
            transform: scale(0.9);
        }

            .slide.active {
                opacity: 1;
                transform: scale(1);
                z-index: 10;
            }

        .slide-1 {
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url('https://images.unsplash.com/photo-1571019613454-1cb2f99b2d8b?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=870&q=80') center/cover;
        }

        .slide-2 {
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url('https://images.unsplash.com/photo-1490645935967-10de6ba17061?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=853&q=80') center/cover;
        }

        .slide-3 {
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)), url('https://images.unsplash.com/photo-1534258936925-c58bed479fcb?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=871&q=80') center/cover;
        }

        .slide h2 {
            font-size: 2.5rem;
            margin-bottom: 20px;
            animation: fadeInDown 1s ease;
        }

        .slide p {
            font-size: 1.2rem;
            max-width: 700px;
            margin-bottom: 30px;
            animation: fadeInUp 1s ease;
        }

        .slider-nav {
            position: absolute;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            z-index: 20;
        }

        .slider-dot {
            width: 12px;
            height: 12px;
            background: rgba(255, 255, 255, 0.5);
            border-radius: 50%;
            margin: 0 5px;
            cursor: pointer;
            transition: background 0.3s;
        }

            .slider-dot.active {
                background: white;
            }

        .slider-arrow {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            background: rgba(255, 255, 255, 0.3);
            width: 50px;
            height: 50px;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            font-size: 1.5rem;
            cursor: pointer;
            z-index: 20;
            transition: background 0.3s;
        }

            .slider-arrow:hover {
                background: rgba(255, 255, 255, 0.5);
            }

        .arrow-left {
            left: 20px;
        }

        .arrow-right {
            right: 20px;
        }

        /* زر ابدأ رحلتك */
        .cta-button {
            display: block;
            width: 250px;
            margin: 30px auto;
            padding: 18px;
            background: #ff6b6b;
            color: white;
            text-align: center;
            border-radius: 50px;
            font-size: 1.3rem;
            font-weight: bold;
            text-decoration: none;
            transition: all 0.3s ease;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
            border: none;
            cursor: pointer;
            animation: pulse 2s infinite;
        }

            .cta-button:hover {
                background: #ff8e8e;
                transform: translateY(-5px);
                box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
            }

        /* قسم حاسبة BMI */
        .bmi-section {
            background: rgba(255, 255, 255, 0.9);
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            margin-bottom: 40px;
            display: none;
        }

            .bmi-section h2 {
                text-align: center;
                color: #2575fc;
                margin-bottom: 20px;
                font-size: 2rem;
            }

        .bmi-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
            max-width: 500px;
            margin: 0 auto;
        }

        .form-group {
            display: flex;
            flex-direction: column;
        }

            .form-group label {
                margin-bottom: 8px;
                font-weight: bold;
                color: #333;
            }

            .form-group input {
                padding: 12px;
                border: 1px solid #ddd;
                border-radius: 8px;
                font-size: 1rem;
            }

        .calculate-btn {
            padding: 12px;
            background: #2575fc;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1.1rem;
            cursor: pointer;
            transition: background 0.3s;
        }

            .calculate-btn:hover {
                background: #1a5fd0;
            }

        .result {
            text-align: center;
            margin-top: 20px;
            font-size: 1.2rem;
            font-weight: bold;
        }

        /* بطاقات النتائج */
        .results-cards {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 20px;
            margin-top: 30px;
        }

        .card {
            width: 300px;
            padding: 20px;
            border-radius: 10px;
            color: white;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            text-align: center;
            display: none;
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 0.5s ease, transform 0.5s ease;
        }

            .card.show {
                opacity: 1;
                transform: translateY(0);
            }

            .card h3 {
                margin-bottom: 15px;
                font-size: 1.5rem;
            }

            .card p {
                margin-bottom: 10px;
            }

        .underweight {
            background: linear-gradient(45deg, #00d2ff, #3a7bd5);
        }

        .normal {
            background: linear-gradient(45deg, #56ab2f, #a8e063);
        }

        .overweight {
            background: linear-gradient(45deg, #ff512f, #dd2476);
        }

        /* برامج التغذية */
        .nutrition-programs {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 20px;
            margin: 40px 0;
        }

        .program-card {
            background: white;
            width: 300px;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s;
        }

            .program-card:hover {
                transform: translateY(-10px);
            }

            .program-card img {
                width: 100%;
                height: 200px;
                object-fit: cover;
            }

        .program-content {
            padding: 20px;
        }

            .program-content h3 {
                color: #2575fc;
                margin-bottom: 10px;
            }

            .program-content p {
                color: #666;
                margin-bottom: 15px;
            }

        .program-btn {
            display: inline-block;
            padding: 8px 16px;
            background: #ff6b6b;
            color: white;
            text-decoration: none;
            border-radius: 20px;
            font-size: 0.9rem;
            transition: background 0.3s;
        }

            .program-btn:hover {
                background: #ff8e8e;
            }

        /* الفوتر */
        footer {
            text-align: center;
            padding: 30px;
            background: rgba(0, 0, 0, 0.7);
            color: white;
            border-radius: 10px;
            margin-top: 40px;
        }

            footer p {
                margin: 5px 0;
            }

        .social-icons {
            margin-top: 15px;
        }

            .social-icons a {
                color: white;
                margin: 0 10px;
                font-size: 1.5rem;
                transition: color 0.3s;
            }

                .social-icons a:hover {
                    color: #ff6b6b;
                }

        /* تأثيرات الحركة */
        keyframes fadeInDown {
            from

        {
            opacity: 0;
            transform: translateY(-30px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        keyframes fadeInUp {
            from

        {
            opacity: 0;
            transform: translateY(30px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        keyframes pulse {
            0%

        {
            transform: scale(1);
        }

        50% {
            transform: scale(1.05);
        }

        100% {
            transform: scale(1);
        }

        }

        /* تصميم متجاوب */
        media (max-width: 768px) {
            .hero-slider

        {
            height: 400px;
        }

        .slide h2 {
            font-size: 2rem;
        }

        .slide p {
            font-size: 1rem;
        }

        .slider-arrow {
            width: 40px;
            height: 40px;
            font-size: 1.2rem;
        }

        .results-cards, .nutrition-programs {
            flex-direction: column;
            align-items: center;
        }

        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>رحلة لياقتك تبدأ هنا</h1>
            <p>اكتشف أفضل الطرق لتحقيق جسم صحي ولياقة بدنية عالية مع برامجنا المتخصصة</p>
        </header>

        <!-- الهيرو مع السلايدر -->
        <div class="hero-slider">
            <div class="slide slide-1 active">
                <h2>لياقة بدنية أفضل</h2>
                <p>تمارين مصممة خصيصًا لتحقيق أهدافك الرياضية بسرعة وأمان</p>
            </div>

            <div class="slide slide-2">
                <h2>تغذية صحية</h2>
                <p>برامج غذائية متوازنة تساعدك على الوصول إلى الوزن المثالي</p>
            </div>

            <div class="slide slide-3">
                <h2>حياة أكثر نشاطًا</h2>
                <p>نصائح وخدع لدمج النشاط البدني في روتينك اليومي</p>
            </div>

            <div class="slider-arrow arrow-left">
                <i class="fas fa-chevron-right"></i>
            </div>

            <div class="slider-arrow arrow-right">
                <i class="fas fa-chevron-left"></i>
            </div>

            <div class="slider-nav">
                <div class="slider-dot active" data-slide="0"></div>
                <div class="slider-dot" data-slide="1"></div>
                <div class="slider-dot" data-slide="2"></div>
            </div>
        </div>

        <button class="cta-button" id="start-journey">ابدأ رحلتك</button>

        <!-- قسم حاسبة BMI -->
        <section class="bmi-section" id="bmi-section">
            <h2>حاسبة مؤشر كتلة الجسم (BMI)</h2>
            <div class="bmi-form">
                <div class="form-group">
                    <label for="weight">الوزن (كجم)</label>
                    <input type="number" id="weight" placeholder="أدخل وزنك بالكيلوجرام">
                </div>
                <div class="form-group">
                    <label for="height">الطول (سم)</label>
                    <input type="number" id="height" placeholder="أدخل طولك بالسنتيمتر">
                </div>
                <button class="calculate-btn" id="calculate">احسب</button>
            </div>

            <div class="result" id="result"></div>

            <div class="results-cards">
                <div class="card underweight" id="underweight-card">
                    <h3>نقص الوزن</h3>
                    <p>مؤشر كتلة الجسم أقل من 18.5</p>
                    <p>ننصحك باستشارة أخصائي تغذية لزيادة وزنك بطريقة صحية</p>
                </div>

                <div class="card normal" id="normal-card">
                    <h3>وزن طبيعي</h3>
                    <p>مؤشر كتلة الجسم بين 18.5 و 24.9</p>
                    <p>حافظ على وزنك الحالي من خلال نظام غذائي متوازن وتمارين منتظمة</p>
                </div>

                <div class="card overweight" id="overweight-card">
                    <h3>زيادة الوزن</h3>
                    <p>مؤشر كتلة الجسم 25 أو أعلى</p>
                    <p>ننصحك ببرنامج تمارين ونظام غذائي لخفض وزنك بطريقة صحية</p>
                </div>
            </div>

            <!-- برامج التغذية -->
            <h2 style="margin-top: 50px;">برامجنا الغذائية</h2>
            <div class="nutrition-programs">
                <div class="program-card">
                    <img src="https://images.unsplash.com/photo-1512621776951-a57141f2eefd?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=870&q=80" alt="برنامج زيادة الوزن">
                    <div class="program-content">
                        <h3>برنامج زيادة الوزن</h3>
                        <p>نظام غذائي صحي لزيادة الوزن وبناء العضلات</p>
                        <a href="#" class="program-btn">اطلع على البرنامج</a>
                    </div>
                </div>

                <div class="program-card">
                    <img src="https://images.unsplash.com/photo-1494390248081-4e521a5940db?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=870&q=80" alt="برنامج الحفاظ على الوزن">
                    <div class="program-content">
                        <h3>برنامج الحفاظ على الوزن</h3>
                        <p>نظام متوازن للحفاظ على وزنك الحالي</p>
                        <a href="#" class="program-btn">اطلع على البرنامج</a>
                    </div>
                </div>

                <div class="program-card">
                    <img src="https://images.unsplash.com/photo-1504674900247-0877df9cc836?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=870&q=80" alt="برنامج خسارة الوزن">
                    <div class="program-content">
                        <h3>برنامج خسارة الوزن</h3>
                        <p>نظام غذائي فعال لخسارة الوزن الزائد</p>
                        <a href="#" class="program-btn">اطلع على البرنامج</a>
                    </div>
                </div>
            </div>
        </section>

        <footer>
            <p>تابعنا على وسائل التواصل الاجتماعي</p>
            <div class="social-icons">
                <a href="#"><i class="fab fa-facebook"></i></a>
                <a href="#"><i class="fab fa-twitter"></i></a>
                <a href="#"><i class="fab fa-instagram"></i></a>
                <a href="#"><i class="fab fa-youtube"></i></a>
            </div>
            <p>جميع الحقوق محفوظة &copy; 2023 - موقع صحتك ولياقتك</p>
        </footer>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function () {
            // السلايدر
            const slides = document.querySelectorAll('.slide');
            const dots = document.querySelectorAll('.slider-dot');
            const arrows = document.querySelectorAll('.slider-arrow');
            let currentSlide = 0;
            let slideInterval = setInterval(nextSlide, 5000);

            function goToSlide(n) {
                slides[currentSlide].classList.remove('active');
                dots[currentSlide].classList.remove('active');

                currentSlide = (n + slides.length) % slides.length;

                slides[currentSlide].classList.add('active');
                dots[currentSlide].classList.add('active');
            }

            function nextSlide() {
                goToSlide(currentSlide + 1);
            }

            function prevSlide() {
                goToSlide(currentSlide - 1);
            }

            dots.forEach(dot => {
                dot.addEventListener('click', function () {
                    const slideIndex = parseInt(this.getAttribute('data-slide'));
                    goToSlide(slideIndex);
                    resetInterval();
                });
            });

            arrows[0].addEventListener('click', function () {
                prevSlide();
                resetInterval();
            });

            arrows[1].addEventListener('click', function () {
                nextSlide();
                resetInterval();
            });

            function resetInterval() {
                clearInterval(slideInterval);
                slideInterval = setInterval(nextSlide, 5000);
            }

            // زر ابدأ رحلتك
            const startButton = document.getElementById('start-journey');
            const bmiSection = document.getElementById('bmi-section');

            startButton.addEventListener('click', function () {
                bmiSection.style.display = 'block';
                bmiSection.scrollIntoView({ behavior: 'smooth' });
            });

            // حاسبة BMI
            const calculateBtn = document.getElementById('calculate');
            const resultDiv = document.getElementById('result');
            const underweightCard = document.getElementById('underweight-card');
            const normalCard = document.getElementById('normal-card');
            const overweightCard = document.getElementById('overweight-card');

            calculateBtn.addEventListener('click', function () {
                const weight = parseFloat(document.getElementById('weight').value);
                const height = parseFloat(document.getElementById('height').value);

                if (isNaN(weight) || isNaN(height) || weight <= 0 || height <= 0) {
                    resultDiv.textContent = 'يرجى إدخال قيم صحيحة للوزن والطول';
                    return;
                }

                // حساب مؤشر كتلة الجسم (BMI)
                const heightInMeter = height / 100;
                const bmi = weight / (heightInMeter * heightInMeter);

                // عرض النتيجة
                resultDiv.textContent = `مؤشر كتلة جسمك هو: ${bmi.toFixed(2)}`;

                // إخفاء جميع البطاقات أولاً
                underweightCard.classList.remove('show');
                normalCard.classList.remove('show');
                overweightCard.classList.remove('show');

                // عرض البطاقة المناسبة بناءً على النتيجة
                if (bmi < 18.5) {
                    setTimeout(function () {
                        underweightCard.classList.add('show');
                    }, 300);
                } else if (bmi >= 18.5 && bmi < 25) {
                    setTimeout(function () {
                        normalCard.classList.add('show');
                    }, 300);
                } else {
                    setTimeout(function () {
                        overweightCard.classList.add('show');
                    }, 300);
                }
            });
        });
    </script>
</body>
</html>



